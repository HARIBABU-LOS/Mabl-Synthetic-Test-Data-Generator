# Mabl-Synthetic-Test-Data-Generator
Accept the raw input from user on different variables or columns with all combination of values for each column, gather the input details, create a .csv file with all combinations
